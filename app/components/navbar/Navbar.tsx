export default function Navbar() {
  // the nav bar should be responsive
  // site name | login | github
  // site name should be replaced with the favicon on mobile
  return (
    <header>
      <nav className="navbar bg-base-100"></nav>
    </header>
  );
}
